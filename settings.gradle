pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

includeBuild('AndroidPlugin')
includeBuild('JavaPlugin')
includeBuild('InternalLibrary')

rootProject.name = "Training sample"
include ':app'
include ':data'
